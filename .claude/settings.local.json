{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if echo \"$f\" | grep -qE '[.]py$'; then cd /Users/muranagakotaro/watch-ai-analyzer && source .venv/bin/activate && python -m pytest tests/ -q 2>&1; fi; } || true",
            "timeout": 30,
            "statusMessage": "Running pytest..."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git:*)",
      "Bash(source:*)",
      "WebFetch(domain:ai.google.dev)",
      "Bash(python3:*)",
      "Bash(wc:*)",
      "Bash(grep:*)",
      "Bash(pip3 install:*)",
      "Bash(ls:*)",
      "Skill(update-config)"
    ]
  }
}
